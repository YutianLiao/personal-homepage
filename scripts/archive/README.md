# 已归档脚本

本目录存放**已停用**的一次性维护工具，不在 `package.json` 中注册。

| 脚本 | 说明 |
| --- | --- |
| `convert-seq2seq.mjs` | 曾从外部 `Notes/seq2seq-tutorial/*.html` 生成 `learning/hello-agent/02-attention/01-seq2seq-tutorial.md`。源 HTML 已不在仓库旁路，章节内容已迁入 `learning/hello-agent/`。若需重跑，须先恢复 HTML 源并手动执行 `node scripts/archive/convert-seq2seq.mjs`。 |
