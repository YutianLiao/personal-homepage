# Demos

交互式 Demo 内容源（类比 `learning/hello-agent/`）。

```
demos/
├── tokenizer/README.md
└── sudoku/README.md
```

页面在 `docs/demos/{id}/index.md`，组件注册在 `docs/.vitepress/theme/index.ts`，Gallery 卡片在 `docs/.vitepress/gallery-sections.ts`。
