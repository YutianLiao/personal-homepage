# Stanford CS336

```
learning/cs336/
├── index.md
└── NN-slug/          # 分部：part.json + assets/ + NN-标题.md
```

编辑本目录，运行 `npm run dev` 同步至 `docs/cs336/`。

新模块注册：在 `docs/.vitepress/learning-modules.json` 添加条目后运行 `npm run build:learning-modules`。
